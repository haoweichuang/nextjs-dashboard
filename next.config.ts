import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   ppr: "incremental",  //Compatible with lint version next-v15.0.0
  // },
};

export default nextConfig;
